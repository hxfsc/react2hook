import * as React from "react"
import { BrowserRouter as Router, Route, Switch } from "react-router-dom"

export default () => {
  return (
    <div>
      <div>React</div>
      <Router>
        <Switch>

        </Switch>
      </Router>
    </div>
  )
}
